---
services:
  gitlab-runner:
    image: gitlab/gitlab-runner:alpine@sha256:03748fbe53bb59a43eb7421bf6e9d3ed50be5fa1af0d0fb4798846037e1e1ea1
    container_name: gitlab-runner-${ENV}
    volumes:
      - ./config/config.toml:/etc/gitlab-runner/config.toml:ro
      - /var/run/docker.sock:/var/run/docker.sock
    restart: unless-stopped

    # trigger for ci = 8